{
  "name": "reshape",
  "description": "A plugin-based html template engine",
  "version": "0.0.0",
  "author": "Jeff Escalante",
  "ava": {
    "verbose": "true",
    "files": [
      "test/index.js"
    ]
  },
  "bugs": {
    "url": "https://github.com/reshape/reshape/issues"
  },
  "dependencies": {
    "babel-code-frame": "^6.11.0",
    "joi": "^9.0.1",
    "lodash.merge": "^4.4.0",
    "reshape-code-gen": "reshape/code-gen",
    "reshape-parser2": "reshape/parser",
    "when": "^3.7.7"
  },
  "devDependencies": {
    "ava": "^0.15.2",
    "coveralls": "^2.11.11",
    "nyc": "^7.0.0",
    "reshape-custom-elements": "reshape/custom-elements",
    "reshape-expressions": "reshape/expressions",
    "posthtml-parser": "^0.2.0",
    "posthtml-render": "^1.0.5",
    "standard": "^7.1.2",
    "sugarml": "reshape/sugarml"
  },
  "engines": {
    "node": ">=6"
  },
  "homepage": "https://github.com/reshape/reshape",
  "keywords": [
    "html",
    "parser",
    "postproccessor",
    "processor",
    "transform"
  ],
  "license": "MIT",
  "main": "lib",
  "repository": "https://github.com/reshape/reshape",
  "scripts": {
    "coverage": "nyc ava && nyc report --reporter=html",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "lint": "standard",
    "test": "npm run lint && nyc ava"
  }
}
